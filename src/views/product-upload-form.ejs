<!DOCTYPE html>
<html lang="en">

<head>
   <%- include('./partials/_head.ejs') %>
   <title>Subir producto - Spice Node</title>
</head>

<body>
   <%- include('./partials/_header.ejs') %>

   <!-- Formulario de carga de productos -->
   <div class="ccFormularioSubirProducto container d-flex flex-wrap flex-column align-items-center">
      <h2 class="pt-5">Bienvenido nombreDeLaPersona</h2>
      <p>Por favor complete la información requerida para cargar un producto</p>
      <div class="ccDetalleSubirProducto mt-5 bg-white shadow p-5">
         <h3>Detalle del producto</h3>
         <form action="/products/create" method="POST" enctype="multipart/form-data">
            <div class="form-row">
               <div class="form-group col-12">
                  <label for="name">Nombre del producto</label>
                  <input type="text" name="name" id="" class="form-control" placeholder="Ej.: Spanish Tacos">
                  <small
                     class="text-danger"><%= (typeof errors != 'undefined' && errors.productName) ? errors.productName.msg : '' %></small>
               </div>
               <div class="form-group col-12">
                  <label for="description">Descripcion</label>
                  <input type="text" name="description" id="" class="form-control"
                     placeholder="Ej.: Este condimento tiene tal y cual cosa">
                  <small
                     class="text-danger"><%= (typeof errors != 'undefined' && errors.productDescription) ? errors.productDescription.msg : '' %></small>
               </div>
               <div>
                  <label for="">Categoría</label>
                  <select name="category">
                     <option value="">Elige una categoría</option>
                     <% for (let i = 0; i < Category.length; i++){ %>
                     <option value="<%= Category[i].id %>  ">
                        <%= Category[i].name %>
                     </option>
                     <%}%>
                  </select>
                  <small
                     class="text-danger"><%= (typeof errors != 'undefined' && errors.productCategory) ? errors.productCategory.msg : '' %></small>
               </div>
               <div class="form-group col-12">
                  <label for="price">Precio</label>
                  <input type="text" name="price" id="" class="form-control" placeholder="Ej.: 100">
                  <small
                     class="text-danger"><%= (typeof errors != 'undefined' && errors.productPrice) ? errors.productPrice.msg : '' %></small>
               </div>
               <div class="form-group col-12">
                  <label for="ingredients">Ingredientes</label>

                  <% for(let i = 0; i < Ingredient.length; i++){ %>

                  <div>
                     <label for="ingredients"><%= Ingredient[i].name %></label>

                     <input type="checkbox" name="ingredients" id="" value="<%= Ingredient[i].id %> ">

                  </div>


                  <% } %>
                  <small
                     class="text-danger"><%= (typeof errors != 'undefined' && errors.productIngredients) ? errors.productIngredients.msg : '' %></small>
               </div>
               <div class="form-group col-12">
                  <label class="text-secondary" for="image">Archivos en PNG o JPEG</label>
                  <input type="file" name="image" class="form-control-file" id="">
               </div>
            </div>
            <button type="submit" class="btn btn-success btn-sm">Confirmar carga</button>
         </form>
      </div>
   </div>

   <%- include('./partials/_bootstrapscripts.ejs') %>
</body>

</html>